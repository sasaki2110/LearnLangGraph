# LangGraph 学習プロジェクト - 依存関係
# このファイルに必要なパッケージを追記し、以下のコマンドでインストールします:
# pip install -r requirements.txt
#
# ⚠️ 重要: Python 3.14に関する制限事項
# langgraph-cli[inmem]は公式にPython 3.13までサポートしています。
# Python 3.14を使用する場合、jsonschema-rsのビルドエラーが発生する可能性があります。
# 推奨: Python 3.13を使用してください。
# 
# Python 3.14でインストールを試みる場合（非推奨）:
# 1. 環境変数を設定: export PYO3_USE_ABI3_FORWARD_COMPATIBILITY=1
# 2. それでもビルドが失敗する場合は、Python 3.13の使用を検討してください

# LangGraph - AIエージェントのオーケストレーションフレームワーク
langgraph>=0.1.0

# LangGraph CLI - Studioを使用するために必要（P25_studio.md参照）
# [inmem]オプションは、メモリベースのチェックポインタを使用する場合に必要
# ⚠️ 公式サポート: Python 3.13まで（Python 3.14は非サポート）
langgraph-cli[inmem]>=0.1.0

# LangChain - LLMやツールとの統合（推奨）
# 注意: Python 3.10以上が必要
langchain>=0.1.0

# LangChain OpenAI統合（OpenAI APIを使用する場合に必要）
langchain-openai>=0.1.0

# 環境変数管理
python-dotenv>=1.0.0

# テスト用パッケージ
pytest>=7.0.0
pytest-asyncio>=0.21.0
pytest-mock>=3.10.0

# プロバイダー固有のパッケージ
# OpenAI APIを使用する場合
openai>=1.0.0
# Anthropic: anthropic

